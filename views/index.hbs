<section class="meeting" id="meeting">
  <div class="meeting_home">
    {{#each details}}
    <div class="me_box1 m_box">
      <h6 class="m_h">{{this.name}}</h6>
      <p class="day">{{this.day}}</p>
      <p class="time">{{this.time}}</p>
    </div>
   
    {{/each}}

  </div>
</section>

